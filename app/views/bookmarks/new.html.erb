<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie, collection: Movie.all, prompt: "Choisir un film" %>
  <%= f.input :comment %>
  <%= f.submit "Ajouter le signet" %>
<% end %>
